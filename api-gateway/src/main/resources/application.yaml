spring:
  application:
    name: api-gateway

app:
  jwt:
    secret: ${JWT_SECRET}

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_DEFAULT_ZONE}
    register-with-eureka: true
    fetch-registry: true

server:
  port: ${API_GATEWAY_PORT}
  
